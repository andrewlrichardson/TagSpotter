class SpotifyController < ApplicationController
  def index
    # if session.key?("spot_user")
    #   spot_user = Spotify::User.new(session[:spot_user])
    #   @songs = spot_user.saved_tracks
    # end
  end
end
